# CursorToolset 功能演示

## 🎯 核心功能

### 1. 列出可用工具集

```bash
$ ./cursortoolset list

📋 可用工具集 (1 个):

1. github-action-toolset (GitHub Action AI 工具集)
   描述: GitHub Actions 构建和调试的 AI 规则工具集，帮助 AI 助手更好地完成 CI/CD 任务
   仓库: https://github.com/shichao402/GithubActionAISelfBuilder.git
   状态: ⏳ 未安装
```

**功能特点：**
- ✅ 显示所有可用工具集
- ✅ 显示工具集详细信息（名称、描述、仓库）
- ✅ 显示安装状态（已安装/未安装）

---

### 2. 安装工具集

#### 安装所有工具集

```bash
$ ./cursortoolset install

📦 开始安装 1 个工具集...

[1/1] 📦 开始安装工具集: GitHub Action AI 工具集
  📥 添加 Git 子模块: https://github.com/shichao402/GithubActionAISelfBuilder.git
  📄 拷贝文件: best-practices.mdc -> .cursor/rules/github-actions/best-practices.mdc
  📄 拷贝文件: debugging.mdc -> .cursor/rules/github-actions/debugging.mdc
  📄 拷贝文件: github-actions.mdc -> .cursor/rules/github-actions/github-actions.mdc
✅ 工具集 GitHub Action AI 工具集 安装完成

✅ 所有工具集安装完成
```

#### 安装特定工具集

```bash
$ ./cursortoolset install github-action-toolset

📦 开始安装工具集: GitHub Action AI 工具集
  📥 添加 Git 子模块: https://github.com/shichao402/GithubActionAISelfBuilder.git
  ...
✅ 工具集 GitHub Action AI 工具集 安装完成
```

**功能特点：**
- ✅ 支持安装所有工具集
- ✅ 支持安装特定工具集
- ✅ 自动克隆 GitHub 仓库
- ✅ 智能处理子模块（失败时回退到直接克隆）
- ✅ 根据 toolset.json 自动拷贝文件
- ✅ 支持通配符匹配文件（如 `*.mdc`）
- ✅ 友好的进度显示和 emoji 图标
- ✅ 源路径不存在时给出友好提示

---

### 3. 清理已安装的工具集 🆕

#### 完全清理（交互式确认）

```bash
$ ./cursortoolset clean

🧹 准备清理以下目录:

  📁 /path/to/.cursor/rules
  📁 /path/to/scripts/toolsets
  📁 /path/to/toolsets

⚠️  此操作将删除已安装的工具集文件。是否继续？ [y/N]: y

  🗑️  删除: /path/to/.cursor/rules
  ⏭️  跳过不存在的目录: /path/to/scripts/toolsets
  🗑️  删除: /path/to/toolsets

✅ 清理完成！共清理 3 个目录
```

#### 强制清理（跳过确认）

```bash
$ ./cursortoolset clean --force

🧹 准备清理以下目录:
  ...

✅ 清理完成！共清理 3 个目录
```

#### 保留工具集目录（只清理安装的文件）

```bash
$ ./cursortoolset clean --keep-toolsets --force

🧹 准备清理以下目录:

  📁 /path/to/.cursor/rules

  🗑️  删除: /path/to/.cursor/rules
  ⏭️  跳过不存在的目录: /path/to/scripts/toolsets

✅ 清理完成！共清理 2 个目录
💡 提示：toolsets/ 目录已保留
```

**功能特点：**
- ✅ 一键清理所有已安装文件
- ✅ 交互式确认，防止误操作
- ✅ `--force` 参数跳过确认
- ✅ `--keep-toolsets` 参数保留仓库目录
- ✅ 清晰的清理进度和结果反馈
- ✅ 自动跳过不存在的目录

---

## 🚀 使用场景

### 场景 1：首次使用

```bash
# 1. 查看可用工具集
./cursortoolset list

# 2. 安装所有工具集
./cursortoolset install

# 3. 确认安装状态
./cursortoolset list
```

### 场景 2：更新工具集

```bash
# 1. 清理旧版本（保留 toolsets 目录以加快重新克隆）
./cursortoolset clean --keep-toolsets --force

# 2. 重新安装（会更新仓库）
./cursortoolset install

# 3. 验证更新
./cursortoolset list
```

### 场景 3：完全卸载

```bash
# 完全清理所有文件（包括 toolsets 目录）
./cursortoolset clean --force
```

### 场景 4：选择性安装

```bash
# 只安装特定工具集
./cursortoolset install github-action-toolset
```

---

## 📦 安装后的文件结构

```
your-project/
├── .cursor/
│   └── rules/
│       └── github-actions/        # AI 规则文件
│           ├── best-practices.mdc
│           ├── debugging.mdc
│           └── github-actions.mdc
├── scripts/                        # 可选脚本目录
│   └── toolsets/
│       └── github-actions/
│           └── gh-action-debug    # 调试工具（如果存在）
└── toolsets/                       # 工具集仓库
    └── github-action-toolset/
        ├── toolset.json           # 工具集描述文件
        ├── core/                  # 核心文件
        └── ...
```

---

## ⚙️ 命令行参数

### install 命令

```bash
cursortoolset install [toolset-name] [flags]

Flags:
  -d, --toolsets-dir string   工具集安装目录（默认: ./toolsets）
  -w, --work-dir string       工作目录（默认: 当前目录）
  -h, --help                  帮助信息
```

### clean 命令

```bash
cursortoolset clean [flags]

Flags:
  -f, --force           跳过确认提示，直接清理
  -k, --keep-toolsets   保留 toolsets/ 目录（只清理安装的文件）
  -h, --help            帮助信息
```

### list 命令

```bash
cursortoolset list

无参数，列出所有可用工具集及其安装状态。
```

---

## 🛡️ 错误处理

### 友好的警告信息

当某些文件不存在时，不会中断安装流程，而是给出友好提示：

```
  ⚠️  跳过目标 scripts/toolsets/github-actions/：源路径不存在
      提示：可能需要先构建工具。请查看工具集文档。
✅ 工具集 GitHub Action AI 工具集 安装完成
```

### 自动回退策略

Git 子模块添加失败时，自动回退到直接克隆：

```
  📥 添加 Git 子模块: https://github.com/...
  ⚠️  子模块添加失败，尝试直接克隆...
  ✅ 克隆成功
```

---

## 🎨 用户体验特性

1. **清晰的进度反馈**
   - 使用 emoji 图标增强可读性
   - 显示当前步骤和总步骤数
   - 实时显示正在执行的操作

2. **智能错误处理**
   - 非致命错误不中断流程
   - 提供具体的错误原因和解决建议
   - 自动尝试备选方案

3. **安全操作**
   - 危险操作（如 clean）默认需要确认
   - 提供 --force 参数给高级用户
   - 清晰显示将要删除的内容

4. **灵活配置**
   - 支持自定义安装目录
   - 支持选择性安装/清理
   - 保留目录结构的选项

---

## 📊 性能

- **安装速度**: 取决于网络速度和仓库大小
- **清理速度**: 几乎瞬时完成（< 1 秒）
- **列表显示**: 瞬时（< 100ms）

---

## ✨ 设计亮点

1. **简单直观**: 三个主要命令，易学易用
2. **自动化程度高**: 读取配置自动完成复杂操作
3. **错误容忍性强**: 部分失败不影响整体流程
4. **可扩展性好**: 易于添加新的工具集
5. **测试完善**: 单元测试 + 集成测试全覆盖

---

## 🔮 未来计划

- [ ] 支持从本地目录安装工具集
- [ ] 支持工具集版本管理
- [ ] 支持工具集依赖关系
- [ ] 支持批量更新所有工具集
- [ ] 添加配置文件支持（.cursortoolsetrc）
- [ ] 支持插件系统

---

**项目完全可用，功能完善，测试充分！** 🎉

